# Core dependencies
fastapi>=0.95.0
uvicorn>=0.21.0
pydantic>=2.0.0
python-multipart>=0.0.6

# MCP SDK
git+https://github.com/modelcontextprotocol/python-sdk.git

# Image processing
pillow>=9.5.0
opencv-python-headless>=4.7.0  # Use headless version to avoid GUI dependencies

# HTTP client
requests>=2.28.0
httpx>=0.24.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0
jinja2>=3.1.2
# numpy>=1.24.0  # Using system package instead
# uuid>=1.30.0   # Problematic package, using built-in uuid module
tqdm>=4.65.0

# Image Generation - Google Gemini API
google-generativeai>=0.3.0

# Network and Service Discovery
zeroconf>=0.39.0
aiohttp>=3.8.4

# 3D Reconstruction - CUDA Multi-View Stereo
open3d>=0.17.0
trimesh>=3.21.0
# pyrender>=0.1.45  # Depends on problematic PyOpenGL, will install separately if needed

# Remote Processing
fastapi-utils>=0.2.1
python-jose>=3.3.0  # For JWT authentication
aiofiles>=23.1.0

# For development
pytest>=7.3.1
black>=23.3.0
isort>=5.12.0
pytest-asyncio>=0.21.0 